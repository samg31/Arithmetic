#include <iostream>
#include "number.hpp"

int main()
{
    Number n;
    Number m;
    std::cout << n.FindSigFigs() << std::endl;
    std::cout << (n == m) << std::endl;
    std::cout << (n != m) << std::endl;
}
